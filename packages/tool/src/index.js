'use strict'

import is from './is'
// import type from './type'
import cache from './cache'
import { LocalStorage, StorageFactory, Storage, SessionStorage } from './storage'
import getQueryParams from './get_query_params'
import buildQueryParams from './build_query_params'

export default {
  is,
  // type,
  cache,
  LocalStorage,
  StorageFactory,
  Storage,
  SessionStorage,
  getQueryParams,
  buildQueryParams
}

export {
  is,
  // type,
  cache,
  LocalStorage,
  StorageFactory,
  Storage,
  SessionStorage,
  getQueryParams,
  buildQueryParams
}
